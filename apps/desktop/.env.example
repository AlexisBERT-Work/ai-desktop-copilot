# CatDesk desktop — variables d'environnement (Vite)
# Copier en `.env` et renseigner pour activer la news (Pilier B).
# Laisser vide désactive proprement la news (l'app fonctionne sans).

# Projet Supabase — news pilotée par l'admin
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
